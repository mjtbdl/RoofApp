<nav class="bg-white shadow-lg sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      
      <!-- Logo and Brand -->
      <div class="flex items-center">
        <a href="/" class="flex items-center space-x-2 group">
          <!-- Logo Icon -->
          <svg class="w-8 h-8 text-forest-600 group-hover:text-forest-700 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          <span class="text-2xl font-bold text-gray-800 group-hover:text-forest-600 transition-colors">RoofApp</span>
        </a>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex md:items-center md:space-x-8">
        <a href="/" class="text-gray-700 hover:text-forest-600 px-3 py-2 rounded-md text-sm font-medium transition-colors">
          Home
        </a>
        <a href="/campgrounds" class="text-gray-700 hover:text-forest-600 px-3 py-2 rounded-md text-sm font-medium transition-colors">
          Campgrounds
        </a>
        <a href="/campgrounds/new" class="text-gray-700 hover:text-forest-600 px-3 py-2 rounded-md text-sm font-medium transition-colors">
          Add Campground
        </a>
      </div>

      <!-- Desktop Auth Section -->
      <div class="hidden md:flex md:items-center md:space-x-4">
        <% if (currentUser) { %>
          <!-- User Menu -->
          <div class="flex items-center space-x-4">
            <span class="text-gray-700 text-sm font-medium">
              Welcome, <span class="text-forest-600 font-semibold"><%= currentUser.username %></span>
            </span>
            <a href="/logout" class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded-lg text-sm font-medium transition-colors">
              Logout
            </a>
          </div>
        <% } else { %>
          <!-- Guest Menu -->
          <a href="/login" class="text-gray-700 hover:text-forest-600 px-4 py-2 rounded-lg text-sm font-medium transition-colors">
            Login
          </a>
          <a href="/register" class="bg-forest-600 hover:bg-forest-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors shadow-md hover:shadow-lg">
            Sign Up
          </a>
        <% } %>
      </div>

      <!-- Mobile menu button -->
      <div class="flex items-center md:hidden">
        <button id="mobile-menu-button" type="button" class="text-gray-700 hover:text-forest-600 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-forest-500 p-2 rounded-md">
          <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-200">
    <div class="px-2 pt-2 pb-3 space-y-1">
      <a href="/" class="block text-gray-700 hover:bg-forest-50 hover:text-forest-600 px-3 py-2 rounded-md text-base font-medium transition-colors">
        Home
      </a>
      <a href="/campgrounds" class="block text-gray-700 hover:bg-forest-50 hover:text-forest-600 px-3 py-2 rounded-md text-base font-medium transition-colors">
        Campgrounds
      </a>
      <a href="/campgrounds/new" class="block text-gray-700 hover:bg-forest-50 hover:text-forest-600 px-3 py-2 rounded-md text-base font-medium transition-colors">
        Add Campground
      </a>
      
      <!-- Mobile Auth -->
      <div class="pt-4 border-t border-gray-200 mt-4">
        <% if (currentUser) { %>
          <div class="px-3 py-2">
            <p class="text-sm text-gray-500">Signed in as</p>
            <p class="text-base font-semibold text-forest-600"><%= currentUser.username %></p>
          </div>
          <a href="/logout" class="block w-full text-left text-gray-700 hover:bg-forest-50 hover:text-forest-600 px-3 py-2 rounded-md text-base font-medium transition-colors mt-2">
            Logout
          </a>
        <% } else { %>
          <a href="/login" class="block text-gray-700 hover:bg-forest-50 hover:text-forest-600 px-3 py-2 rounded-md text-base font-medium transition-colors">
            Login
          </a>
          <a href="/register" class="block bg-forest-600 hover:bg-forest-700 text-white px-3 py-2 rounded-md text-base font-medium transition-colors mt-2 text-center">
            Sign Up
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>